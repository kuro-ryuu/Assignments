const a = Number(prompt("Enter the first number: "))
const b = Number(prompt("Enter the second number: "))
const p = Number(prompt("Enter the third number: "))


const sum = a + b + p;
const avg = sum / 3;
const product = a * b * p;

console.log("Sum: " + sum + "\nAverage: " + avg + "\nProduct: " + product)
